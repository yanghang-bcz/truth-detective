# 性能优化说明（2026-09-11）

## 一句话结论

画面几乎没变，但每一帧的绘制调用从 **3950 次降到 298 次**，场景加载从 **2.39 秒降到 1.05 秒**，
帧耗时从 **7.52ms 降到 6.12ms**（60fps 的预算是 16.67ms，现在有 2.7 倍余量）。
固定相机的 `size` 从 34 调到 28，画面比原来近 21%。

## 问题出在哪

用 `Godot --headless --script res://tools/diag.gd` 量出来的结论：

- 整个街区是一个生成脚本（`tools/build_scene.gd`）拼出来的，结果是 **1340 个各自独立的
  `MeshInstance3D`**，每个自带一份独立网格。一帧要提交 3950 次绘制调用（含阴影 pass），
  在 Metal 上光提交就吃掉十几毫秒 —— 而总面数其实只有 30 万，GPU 一点都不累。
  **瓶颈是绘制次数，不是多边形数量。**
- 校园 / 咖啡馆两个 Blender 模型里的**招牌文字**占了全场景 64% 的三角面
  （"Engraved gate name" 单个就 6.1 万面），在固定远景下只有几个像素。
- 碰撞体是用所有渲染网格的三角面拼出来的，含玩家根本够不到的招牌、窗棂、遮阳篷，
  生成出 **24MB 的单行文本**（约 30 万三角面），拖慢加载并占用大量内存。
- 环境开了 **SSIL**（Godot 里最贵的后处理，逐像素光线步进），实测 1.25ms，
  在这种大色块的风格化画面上肉眼几乎分辨不出来。

## 改了什么

### 1. 静态几何体按材质合并（收益最大）
`tools/build_scene.gd` 新增 `merge_static_geometry()`：把所有静态网格按
「材质实例 + 顶点属性格式」分组，顶点烘焙到世界空间，合并成 66 个多表面批次。

- 合并节点放在原点、单位变换，所以依赖世界坐标的着色器（`wet_asphalt`）照旧正确。
- **不合并** `cast_shadow = OFF` 的节点（边界雾层、灯笼光锥）——它们的着色器依赖自身
  局部坐标，合并会算错。
- 不合并带骨骼 / 形态键的网格。

### 2. 碰撞体在合并前单独烘焙
合并之后就分不出单个物体了，所以碰撞几何体的生成也搬到 `build_scene.gd`，
新增 `build_collision_shape()`，在合并之前执行，并输出到
`scenes/neighborhood_collision.res`（2.4MB 二进制，原来是一行 24MB 文本）。
筛选规则：玩家是 1.5 米高的胶囊，**整块都在 1.55 米以上的物体直接不进碰撞体**
（招牌字、窗棂、遮阳篷、女儿墙），对角长度小于 0.22 的碎屑也不进（落叶、漆线、排水槽）。

`tools/build_playable.gd` 相应改为直接加载这个 `.res`，不再遍历渲染网格现算。

### 3. 光照与后处理
- **月光阴影**从 4 级级联（4 遍阴影渲染）改为单张正交阴影图。固定正交相机下所有物体
  距离相近，级联没有精度收益，改完 pass 数除以 4、清晰度反而更均匀。
- **关掉 SSIL**（省 1.25ms，肉眼几乎无效）。SSAO / 辉光 / 体积雾 / 4 个局部雾体积 /
  4 盏灯笼的投影与体积雾贡献**全部保留**，画质优先。
- 灯笼聚光灯的投影**保留**：实测只花 0.26ms（几何合并后每遍阴影只剩几十次绘制）。

### 4. 相机
`Size` 34 → 28。这是量出来的：用 `tools/check_framing.gd` 把玩家能走到的四个边界角落
投影到屏幕，34 时画面还空着 27% 余量（所以显得远），28 时近 21% 且角落仍有 11.6% 余量；
降到 26 只剩 4.8%，24 就直接出画了。

## 怎么重新生成场景

**改完 `tools/build_scene.gd` 或 `build_playable.gd` 之后必须按顺序重跑两步**：

```bash
# 注意：当前 Godot 编辑器在 /tmp 下，重启电脑后会消失，需要从 Downloads 或原压缩包重新解压
GODOT=/tmp/truth-godot-runtime/Godot.app/Contents/MacOS/Godot
cd <项目目录>
"$GODOT" --path "$PWD" --headless --script res://tools/build_scene.gd     # 生成 neighborhood + 碰撞体
"$GODOT" --path "$PWD" --headless --script res://tools/build_playable.gd  # 生成可玩场景
```

顺序不能颠倒，`build_playable.gd` 依赖前一步产出的 `neighborhood_collision.res`。

⚠️ 注意：`build_scene.gd` 的入口必须用 `call_deferred("build")` 延迟一帧。
在 `_initialize()` 里直接建场景时根节点还没真正入树，`global_transform` 会全部退化成
单位矩阵（踩过这个坑：89 万次报错、几何全部塌到原点）。

## 工具脚本（都在 tools/ 下）

| 脚本 | 用途 |
|---|---|
| `bench.gd` | 真实帧耗时基准。用 `force_draw` + `force_sync` 绕开垂直同步——**FPS 会被显示链路钳制在刷新率上，量不出 60fps 以上的差异，不要用 FPS 判断性能** |
| `bench_features.gd` | 同一进程内逐项开关渲染特性，量出每一项的真实成本 |
| `bench_load.gd` | 场景加载耗时拆分（解析 / 实例化 / 物理就绪） |
| `check_framing.gd` | 把玩家可达边界投影到屏幕，判断某个正交 size 下会不会出画 |
| `diag.gd` | 统计每个网格的三角面数，定位几何成本来源 |
| `validate_scene.gd` | 场景结构校验（已更新为校验「合并后应为少量批次」） |
| `test_player.gd` | 12 项玩法回归测试，改碰撞体后必跑 |

## 当前各项特效实测成本（1280×800，Apple M3）

| 项 | 成本 |
|---|---|
| SSIL | 1.25ms（已关） |
| SSAO | 0.84ms |
| 体积雾 | 0.78ms |
| 辉光 | 0.46ms |
| 灯笼光柱着色器 | 0.40ms |
| 灯笼阴影 | 0.26ms |
| 月光阴影 pass | 0.25ms |
| 4 级级联（相对单张） | 0.25ms |
| 尘埃粒子 | 0.08ms |
| 局部雾体积 | 0.10ms |

## 还能继续做的（按性价比排序）

1. **Blender 里把招牌文字减面**：6 个文字网格占全场景 64% 的三角面，在固定远景下
   只有几个像素。导出前 Decimate 到 5%，外观基本无变化，能进一步降顶点处理和碰撞体体积。
2. **首帧着色器编译顿挫**：Godot 4 遇到新材质会现场编译管线，表现为开局前几秒一卡一卡。
   可以在加载画面里把主要材质先渲染到屏幕外预热一遍。
3. **渲染缩放**：项目设置里加 `rendering/scaling_3d/mode=1`（FSR1）+
   `scale=0.85`，能再省约 25% 填充率。当前余量够用，没开。
4. **CPUParticles3D → GPUParticles3D**：6 组共 600 个粒子目前在 CPU 上算，
   量不大，但后续粒子变多时值得换。
5. **注意 MacBook Air 无风扇**：持续负载几分钟后 M3 会降频，长时间游玩帧率会自然下滑，
   这是硬件特性，不是代码问题。
