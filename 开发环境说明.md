# 开发环境说明

> 这份文档只回答一个问题：**怎么在这台机器上把《Truth Detective》跑起来、改起来、导出模型。**
> 工程结构、玩法设计、性能数据请看 `README_中文.md` 和 `性能优化说明.md`。

---

## 1. 工程在哪

| | 路径 |
|---|---|
| **唯一真源** | `~/Documents/GitHub/truth-detective` |
| 远端仓库 | https://github.com/yanghang-bcz/truth-detective.git |

所有修改都在这个目录里做。`.codex/.chatgpt-projects/...` 下那份**已经改成指向这里的软链接**，
两个路径打开的是同一批文件，不会再出现「我改了一份、你跑的是另一份」。

---

## 2. 一条命令搞定一切

工程根目录下的 `godot.sh` 和 `blender.sh` 会自动定位编辑器和工程目录，
**不用记绝对路径，也不用管编辑器装在哪。**

```bash
cd ~/Documents/GitHub/truth-detective

./godot.sh                      # 打开 Godot 编辑器（日常开发入口）
./godot.sh --run                # 直接运行游戏，等于编辑器里按 F5
./godot.sh --rebuild            # 重建场景（改了生成脚本后必须跑）
./godot.sh --test               # 玩法回归测试 + 场景校验
./godot.sh --bench              # 性能基准（真实帧耗时）

./blender.sh                    # 打开 blender/detective.blend
./blender.sh --rebuild-detective   # 重新程序化生成并导出侦探模型
```

在 VS Code 里：`Cmd+Shift+P` → `Tasks: Run Task` → 上面每一项都有对应的任务。

> 编辑器定位顺序：`$GODOT_BIN` → `~/Applications/Godot.app` → `/Applications/Godot.app` → `$PATH` 里的 `godot`。
> Blender 同理（`$BLENDER_BIN` → `/Applications/Blender.app` → `$PATH`）。

---

## 3. Godot 侧

| 项 | 值 |
|---|---|
| 版本 | **4.5.1 stable**（Metal 后端，Forward+ 渲染器） |
| 编辑器位置 | `~/Applications/Godot.app`（从 `/tmp` 复制过来的持久副本，重启不会丢） |
| 主场景 | `res://scenes/playable_neighborhood.tscn` |

### 改代码前必须知道的 5 条约定

1. **场景是脚本生成的，不要手改 `.tscn`。**
   改 `tools/build_scene.gd` / `tools/build_playable.gd`，然后跑 `./godot.sh --rebuild`。
   顺序不能反——`build_playable.gd` 依赖 `build_scene.gd` 产出的 `scenes/neighborhood_collision.res`。
2. **生成脚本的入口必须 `call_deferred("build")`。**
   在 `_initialize()` 里直接建，根节点还没入树，`global_transform` 全部退化成单位矩阵，
   几何会整体塌到原点。
3. **静态几何体在生成时按材质自动合并**（`merge_static_geometry()`），
   1331 个网格 → 75 个批次。不要往回加独立的 `MeshInstance3D`，否则绘制调用会重新爆掉。
   `tools/validate_scene.gd` 有断言守着（合并后应在 30~200 之间）。
4. **改碰撞体后必跑** `./godot.sh --test`（12 项玩法回归）。
5. **性能不要看 FPS**（会被垂直同步钳在 60），用 `./godot.sh --bench` 的 `frame_ms_median`。

---

## 4. Blender 侧

| 项 | 值 |
|---|---|
| 版本 | **Blender 4.5.3 LTS** |
| 位置 | `/Applications/Blender.app` |
| 建模文件 | `blender/detective.blend` |
| 生成脚本 | `blender/build_detective.py` |

### ⚠️ 最重要的一条

**`blender/detective.blend` 是产物，不是真源。**
侦探角色由 `build_detective.py` 程序化生成（几何、材质、骨骼、Idle/Walk/Run 三段动画全部写在脚本里）。

- 在 Blender 里手动改了 `.blend` → **不会进游戏**，下次跑脚本就被覆盖
- 要改角色 → 改 `build_detective.py` → 跑 `./blender.sh --rebuild-detective`

### 模型管线

```
blender/build_detective.py          ← 改这里
        │  ./blender.sh --rebuild-detective
        ▼
assets/characters/detective.glb     ← 产物，Godot 自动导入
        ▼
scenes/detective.tscn 的 Visual 节点（由 tools/build_playable.gd 组装）
```

### 场景里的建筑 / 街道模型

咖啡馆、学校、路灯不是导入的 `.tscn`，而是在运行时用 `GLTFDocument` 直接读 GLB
（见 `build_scene.gd` 的 `refined()`）：

```gdscript
var cafe = refined("CornerCafe", "cafe_refined", Vector3(8.7, 0, -3.9))
#                        ↑ 节点组名    ↑ assets/models/<这个名字>.glb
```

**新增一个模型**：把 GLB 导出到 `assets/models/`，然后在 `build_scene.gd` 里加一行 `refined(...)`，
跑 `./godot.sh --rebuild`。

### 坐标与比例约定

- **1 单位 = 1 米**，比例别乱动
- 角色总高 **1.598 m**，三角面 2740，骨骼 7 根
- **原点放在脚底**（不是几何中心），这样在 Godot 里 `position.y = 0` 就是站在地面上
- 正面朝向：Blender 的 **-Y** 方向 → 导出后对应 Godot 的 **+Z**

---

## 5. 排错

| 现象 | 原因 / 处理 |
|---|---|
| `找不到 Godot 编辑器` | 把 `Godot.app` 放进 `/Applications`，或 `export GODOT_BIN=/path/to/Godot` |
| 改了生成脚本但游戏没变 | 忘了跑 `./godot.sh --rebuild` |
| 场景里几何全塌到原点 | 生成脚本入口没写 `call_deferred("build")` |
| 绘制调用从 300 涨到 3000+ | 合并步骤被破坏了，跑 `./godot.sh --test` 看 `validate_scene` 的断言 |
| Blender 导出报 `'Scene' object has no attribute 'gltf_action_filter'` | Blender 4.5 在无界面模式下的已知噪音，**不影响导出**，可以忽略 |
| Godot 编辑器里看不到新场景 | 切回编辑器时让它重新导入一次；或 `Godot --path . --headless --import` |
