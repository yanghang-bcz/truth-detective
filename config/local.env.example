# Truth Detective — 本地 AI 配置示例
#
# 用法：复制成 config/local.env，填入你自己的 key。
#     cp config/local.env.example config/local.env
#
# config/local.env 已在 .gitignore 里，**不会被提交**。
#
# 也可以完全不建这个文件，改用系统环境变量（推荐，key 不落盘）：
#     export DEEPSEEK_API_KEY=sk-xxxxxxxx
#
# 没有 key 也完全能玩：AI 分析员不可用时，四个动作会自动退回本地规则引擎，
# 而且不消耗 AI 咨询次数。见 scripts/ai/ai_coach.gd。

DEEPSEEK_API_KEY=your_key_here
