# 让 Godot 完全跳过这个目录。
#
# 这里是截图产物（中英双语各 9 张 1600×1000 的 PNG，加起来二十多兆），
# 只在开发时用眼睛看，运行时一行都不会加载。
# 不加这个文件的话，编辑器会把它们当普通贴图全部导入 —— 启动扫描变慢、
# 导入缓存里多出二十多兆、有些还会被压缩成显存纹理。纯粹是白花的时间。
#
# 注意：.gdignore 只影响编辑器的扫描与导入，不影响运行时的 FileAccess，
# 所以截图工具照旧能往 res://previews/ 里写文件。
